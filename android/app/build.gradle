plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.actas_sena_mobile"
    compileSdk = 34 // ← CAMBIADO de flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.example.actas_sena_mobile"
        minSdk = 21  // ← CAMBIADO de flutter.minSdkVersion
        targetSdk = 34  // ← CAMBIADO de flutter.targetSdkVersion
        versionCode = 1  // ← CAMBIADO de flutter.versionCode
        versionName = "1.0.0"  // ← CAMBIADO de flutter.versionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}